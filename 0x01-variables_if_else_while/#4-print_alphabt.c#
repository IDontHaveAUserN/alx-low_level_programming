#include <stdio.h>
#include <stdlib.h>
/**
* main - entry point
* Return: Always 0 (success)
*/

int main(void)
{
  char i;
r (i = 'a'; i <= 'z'; i++)
if (i != 'q' && i != 'e')
putchar(i);

putchar('\n');

return (0);
}
